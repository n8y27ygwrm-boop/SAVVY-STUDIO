# Namecheap Private Email SMTP credentials for visitor confirmation delivery
# Configure these in your Vercel Project Settings -> Environment Variables.
# NEVER commit actual production secrets to source control.

PRIVATE_EMAIL_USER=hello@savvystudio.cc
PRIVATE_EMAIL_PASSWORD=your_private_email_password_here
